import 'package:flutter/material.dart';

class MyAdminCourseScreen extends StatelessWidget {
  const MyAdminCourseScreen({super.key});

  static const routeName = "my-admin-course";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
